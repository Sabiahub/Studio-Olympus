process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0'; const fs = require('fs'); const env = fs.readFileSync('.env', 'utf-8'); const host = env.match(/NEXT_PUBLIC_SUPABASE_URL=(.*)/)[1].trim(); const url = 'https://' + host + '.supabase.co'; const key = env.match(/NEXT_PUBLIC_SUPABASE_ANON_KEY=(.*)/)[1].trim(); fetch(url + '/rest/v1/studio?select=id', { headers: { apikey: key, Authorization: 'Bearer ' + key } }).then(r => r.json()).then(data => { if(data.length > 1) { const keepId = data[0].id; const deleteIds = data.slice(1).map(d => d.id); fetch(url + '/rest/v1/studio?id=in.(' + deleteIds.join(',') + ')', { method: 'DELETE', headers: { apikey: key, Authorization: 'Bearer ' + key } }).then(() => console.log('Deleted duplicates:', deleteIds)); } else { console.log('No duplicates.'); } });
